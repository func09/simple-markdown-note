import { Dimensions } from "react-native";

// ---------------------------------------------------------------------------
// Data & Operations
// ---------------------------------------------------------------------------

/**
 * 自動保存が発火するまでの遅延時間 (ms)
 * 入力が止まってからこの時間が経過した後にサーバーへ同期します。
 */
export const AUTO_SAVE_DELAY = 1000;

// ---------------------------------------------------------------------------
// UI & Animations
// ---------------------------------------------------------------------------

/**
 * アクション後の画面遷移までの遅延時間 (ms)
 * 例：削除ダイアログが閉じた後、一覧画面へ戻るまでの待機時間などに使用します。
 */
export const NAVIGATION_DELAY = 250;

/**
 * ドロワーコンポーネントの幅
 */
const { width: SCREEN_WIDTH } = Dimensions.get("window");

/**
 * ナビゲーションドロワーの表示幅。画面幅の80%に設定されています。
 */
export const DRAWER_WIDTH = SCREEN_WIDTH * 0.8;

/**
 * ドロワー開閉スライドアニメーションの所要時間 (ms)
 */
export const DRAWER_ANIM_DURATION = 300;

/**
 * 入力フォーカス実行時の遅延時間 (ms)
 * キーボードやUIのレンダリング完了を待ってからフォーカスを当てるために使用します。
 */
export const FOCUS_DELAY = 50;
