import { VerifyEmailScreen } from "@/features/auth/components/VerifyEmailScreen";
/**
 * メールの認証処理を行う画面のルートページコンポーネント。
 * APIを利用したトークン検証処理を呼び出します。
 */
export default function VerifyEmailPage() {
  return <VerifyEmailScreen />;
}
